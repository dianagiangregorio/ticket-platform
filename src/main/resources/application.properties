spring.application.name=ticket-platform

#data source

spring.datasource.url=jdbc:mysql://localhost:3306/db_ticket_platform
spring.datasource.username=root
spring.datasource.password=Pennino@98

## Hibernate Properties

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = validate

#! Impostazioni aggiuntive, utili per il testing (rimuovere)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.web.resources.static-locations=classpath:/static/,classpath:/public/,classpath:/webjars/
